# 需求:右鍵點一下,把當下 hover 的座標與 RGB 記錄下來

> `/user` 階段產物:我只講「我想要什麼、為什麼」。怎麼實作交給 PO/architect。

## 1. 痛點

現在 hover 顯示的座標跟 RGB 只在游標停在那個點的當下才看得到,游標一移開就沒了。
我常常需要**比較兩個點**(例如可疑區域 vs 背景)的像素值,但移動滑鼠去看第二個點,
第一個點的數字就從畫面上消失了,我得自己記住或另外抄下來。

## 2. 我想要的

- 在主圖上**滑鼠右鍵點一下**,把「當下游標所在的座標與 RGB」**記錄並固定顯示**在畫面上,
  不會因為我把滑鼠移開就消失。
- 畫面上要看得出**記錄的點在圖上的哪裡**(不是只有文字數字,要能對到圖上的位置)。
- 再右鍵點別的地方 → 記錄的點**換成新的**(我不需要同時留很多個點,一次看一個就好)。
- 切下一張圖 → 之前記錄的點自然清掉(那是上一張圖的東西,沒必要留著)。

## 3. 我不在乎 / 可放棄

- 不需要把記錄的點存檔或匯出;這只是我當下比對用的,不用跨 session 保留。
- 不需要同時記錄多個點、也不需要清除鈕——右鍵點新位置直接覆蓋、切圖直接清空,對我來說夠用。
